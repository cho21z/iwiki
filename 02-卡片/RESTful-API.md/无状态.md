> 💡 什么叫"无状态"？为什么 RESTful API 要求服务端不保存会话上下文？

## 无状态（Stateless）
每次请求都是独立的，需要携带本次处理所需的全部信息，比如用户身份、请求参数等，服务端不依赖上一次请求的上下文来处理本次请求。

例如认证通常是在请求头里带上 Token，由服务端基于 Token 识别用户身份，而不是依赖"上一次请求记住你是谁"。

## 为什么要无状态？
- 更利于横向扩展：任意一台服务器都能处理任意一次请求
- 更容易做负载均衡：不用考虑会话粘连
- 更方便排查问题：每个请求都是自包含的
- 更适合微服务与云原生架构
